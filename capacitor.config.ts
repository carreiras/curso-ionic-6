import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'curso-ionic-6',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
